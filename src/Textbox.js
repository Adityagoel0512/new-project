import React from "react";
import "./Textbox.css";

const TextBox = (props) => {
    return (
        <div className="Container2">
            {props.children}
        </div>
    )
}
export default TextBox;
